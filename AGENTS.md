IMPORTANT:

This project may be using a modified or newer Next.js version with breaking changes.

Do not assume:
- App Router works exactly like older versions
- File structure is standard (/pages, /app, routing conventions may differ)
- APIs such as fetch, middleware, server actions, config, or data fetching behave as expected
- Older patterns from training data are still valid

Before writing or editing code:

1. Read documentation inside:
   node_modules/next/dist/docs/

2. Check:
- Current API usage
- Updated conventions
- Deprecated features
- Breaking changes warnings
- Required migration patterns

3. Follow the installed Next.js version as source of truth,
not model memory.

4. If you see deprecation notices, update code to the recommended replacement instead of using legacy patterns.
